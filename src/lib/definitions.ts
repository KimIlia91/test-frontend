export type User = {
    id: string
    name: string
    email: string
    image: string
    password: string
}

export type UserProfile = {
    id: string
    name: string
    email: string
    image: string
}

export type PartnersTable = {
    id: string
    name: string
    email: string
    phone: string
    image: string
    description: string
    is_favorite: boolean
}

export type Partner = {
    id: string
    name: string
    email: string
    phone: string
    image: string
    description: string
}